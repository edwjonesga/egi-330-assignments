public class BusinessAccount extends BankAccount {
    // Step 1: Create a constructor for BusinessAccount that calls the parent constructor
    public BusinessAccount(String accountHolder, double initialBalance) {
        // call the super constructor
    }

    // Step 2: Override calculateInterest to apply a 5% interest rate
    // public double calculateInterest() {}
}
