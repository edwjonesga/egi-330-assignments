public class CheckingAccount extends BankAccount {
    // Step 1: Create a constructor for CheckingAccount that calls the parent constructor
    public CheckingAccount(String accountHolder, double initialBalance) {
        // call the super constructor
    }

    // Step 2: Override calculateInterest to apply a 1% interest rate
    // public double calculateInterest() {}
}
